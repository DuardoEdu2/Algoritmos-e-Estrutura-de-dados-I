//Tipo_abstrato_de_dados

/*
//como printf é implementado? (stdioC)

Ideia central:
    -encapsular de quem usa um determinado tipo a dorma concreta com que ele foi implementado
Beneficios:
    -

                                        //modulos e compilação em separado

Modulo
    -arquivo com funções que representam parte de uma implementação.
    -A implementação de um programa pode usar u ou mais modulos.
        *programa composto por varios módulos:
            -cada um compilado separadamente (.o ou .obj);
            -o ligador (linker) reune todos os arquivos objeto em um único arquivo executavel.
        *para programas de médio e grande porte, a sua divisão em vários módulos é funfamental.

*/

////Tipo_abstrato_de_dados
    /*
        O que é TAD?
            TAD: um novo tipo de dado (struct) + conjunto de operações*/